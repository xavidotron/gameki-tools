#!/bin/bash

set -e

run="${1:-0}"

dir="$(mktemp -d)"
mkdir -p "$dir/toolate"

Gameki/bin/prod -r "$run" -p "$dir/toolate/" Handouts/gm-notes.tex Handouts/calendar.tex Handouts/events.tex

Gameki/bin/book --run "$run" --pdf-dir "$dir/toolate/" --names >&2

(cd "$dir" && zip -r - "toolate")
